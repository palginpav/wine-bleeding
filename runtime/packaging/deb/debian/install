# Files are installed via 'make install' in debian/rules.
# This file is intentionally empty; all paths are handled by the Makefile
# install target using install -D to create parent directories automatically.
